Perficient Blog Markdown Archive

Each post is exported as a page bundle:
- markdown/<slug>/index.md
- markdown/<slug>/images/...

The Markdown format is CommonMark-style with YAML front matter and relative image paths for portability into static site generators and other content systems.
